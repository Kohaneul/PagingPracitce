<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.web.MemberRepository">
        <select id="findAll" resultType="com.example.demo.domain.Member" parameterType="com.example.demo.dto.Pagination" >
            SELECT a.* FROM
            ( SELECT @ROWNUM :=@ROWNUM+1 AS rownum, m.*
                FROM member m,(select @rownum :=0) tmp) a
            limit #{recordSize}
            offset #{offset};
        </select>

    <select id="count" resultType="int">
        select count(*) from member;
    </select>



</mapper>